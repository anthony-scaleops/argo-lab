apiVersion: apps/v1
kind: Deployment
metadata:
  name: nginx-no-annotation
  labels:
    app: nginx-no-annotation
spec:
  replicas: 1
  selector:
    matchLabels:
      app: nginx-no-annotation
  template:
    metadata:
      labels:
        app: nginx-no-annotation
    spec:
      containers:
      - name: nginx
        image: nginx:stable
        ports:
        - containerPort: 80